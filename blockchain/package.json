{
  "name": "pravah-blockchain",
  "version": "1.0.0",
  "description": "Blockchain audit trail for Pravah document management",
  "scripts": {
    "deploy": "hardhat run deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
